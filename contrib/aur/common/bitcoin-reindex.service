[Unit]
Description=Bitcoin Daemon (reindex)
Conflicts=bitcoin.service
After=network.target

[Service]
User=bitcoin
Group=bitcoin
ExecStart=/usr/bin/dogecashd -daemon=0                       \
                            -conf=/etc/bitcoin/bitcoin.conf \
                            -datadir=/srv/bitcoin           \
                            -reindex                        \
                            -pid=/run/bitcoin/dogecashd.pid
ExecReload=/usr/bin/kill -HUP $MAINPID
ExecStop=/usr/bin/dogecashd stop
PrivateTmp=true

[Install]
WantedBy=multi-user.target
